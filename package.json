{
  "name": "shape-constructor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aframe": "^0.8.2",
    "aframe-react": "^4.4.0",
    "axios": "^0.18.0",
    "color-convert": "^1.9.3",
    "react": "^16.7-alpha",
    "react-dom": "^16.7-alpha",
    "react-scripts": "2.1.1",
    "sbd": "^1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "now": "now ./build -A ../now.json --public && now alias",
    "deploy": "yarn run build && yarn run now"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
